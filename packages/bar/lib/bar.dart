library bar;

import 'package:flutter/material.dart';

class Bar extends StatelessWidget {
  const Bar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('Bar', style: TextStyle(fontSize: 24));
  }
}
